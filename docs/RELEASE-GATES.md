# Nyrathen v5.4 — Release Gates

The authoritative gate specification is `docs/RELEASE-GATES-v5.4.md`.

Use `npm run release:certify` for all locally executable release qualification and `REQUIRE_EXTERNAL_EVIDENCE=true npm run release:evidence` before a production/store launch. External hardware, store-account and infrastructure evidence is never inferred from local tests.
